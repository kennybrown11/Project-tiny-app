<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
  <body>
    <h4> Index of URLs</h4>

    <% for (url in urls) { %>
    <form method="POST" action="/urls/<%= url %>/delete">
      <p>
        <%= `${url} ---> ${urls[url]}` %>
        <a href="/urls/<%= url %>">Update</a>
        <input type="submit" value="Delete">
      </p>
    </form>
    <% }; %>
    <a href="/urls/new">Create new URL</a>
  </body>
</html>
