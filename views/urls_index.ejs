<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
  <body>
    <% include partials/_header.ejs %>
    <h4> Index of URLs</h4>

    <% for (url in urls) { %>
      <form method="POST" action="/urls/<%= url %>/delete">
    <p><%= `${url} ---> ${urls[url]}` %>
      <a href="/urls/<%= url %>">Edit</a><input type="submit" value="Delete"></p>
      <% }; %>
    </form>
      <a href="http://localhost:8080/urls/new">
      Add new Link</a>  
  </body>
</html>
