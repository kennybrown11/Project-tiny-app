<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
  <body>
      <% include partials/_header.ejs %>
  <h4><%= `ShortURL: ${shortURL}` %></h4>
  <a href="/u/<%= shortURL%>">http://localhost:8080/u/<%= shortURL %></a>
  <form method="POST" action="/urls/<%= shortURL %>/update"
          <p>
            <label for "long-url-input"></label>
            <input id="longURL" name="longURL" value= "<%= `${urls[shortURL]}` %>" placeholder="longURL" style="width: 300px">
            <input type="submit" value="Update"> 
          </p> 
    </form>
    <a href="http://localhost:8080/urls/">
      Index 
    </a>  
  </body> 
</html>
