
<!DOCTYPE html>

<html lang="en">
  <head>
    <title>Tiny App</title>
    <% include partials/_header.ejs %>
  </head>

  <body>
      <% if (user) { %> 
    <tt>    
    <p><a href="/u/<%= shortURL%>"><%= shortURL %></a></p>

    <form action="/urls/<%= shortURL %>" method="post">
      <p>
        <input id="longURL" name="longURL" value="<%= urlObj.longURL %>" style="width: 250px">
        <button>Update</button>
      </p>
      <p><a href="/urls">Back to tinyURLS</a></p>
    </form>
      </div><% } else{ %><a href="/register">Register</a><a href="/login">Login</a><% } %>
    </tt>
    </body>
</html>
